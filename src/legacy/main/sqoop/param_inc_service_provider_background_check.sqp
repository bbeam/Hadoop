--query
'SELECT ServiceProviderBackgroundCheckId, ServiceProviderId, FirstName, LastName, ProfileNumber, CompletedDate, BackgroundCheckStatusId, CreateBy, CreateDate, UpdateBy, UpdateDate, CustomerId FROM Angie.dbo.ServiceProviderBackgroundCheck WHERE $CONDITIONS'
--split-by
ServiceProviderBackgroundCheckId
--hive-delims-replacement
'#'
--fields-terminated-by 
'\001' 
--lines-terminated-by 
'\n'
--optionally-enclosed-by 
'\"'
--null-string 
'' 
--null-non-string 
''